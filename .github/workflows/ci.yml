name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show toolchain versions
        run: g++ --version && make --version
      - name: Build
        run: make all
      - name: Run tests
        run: make test
